{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "configuration": {
      "description": "The configuration option we use when running the app.",
      "type": "string"
    }
  }
}
